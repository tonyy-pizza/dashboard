__pycache__/
*.pyc
.pytest_cache/
fonts/

# Collector output
cache.json

# Dashboard-owned local data — these live next to the scripts on joputer
todo.json
habits.json
rough_notes.txt

# Secrets — never commit these. calendar_url.txt holds the private iCal
# address; credentials.json/token.json are leftovers from the old OAuth setup.
calendar_url.txt
credentials.json
token.json
